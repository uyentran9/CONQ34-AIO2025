---
layout: post
title: Module 6 – Tuần 2
date: 10-11-2025
categories: [AIO2025, Module6, Deep Learning]
use_math: true
---

{% include mathjax.html %}

## Tóm tắt bài giảng ngày thứ Tư ngày 12/11/2025 

## 📈 Phần 1 – Ôn lại Linear Regression và Logistic Regression

# Linear Regression 

**Linear Regression** là mô hình học có giám sát dùng để dự đoán giá trị liên tục 

**Mục tiêu** là mô hình Linear Regression sẽ **cập nhật các trọng số w và hệ số bias b** qua mỗi lần huấn luyện sao cho giá trị $L_{\text{MSE}}$ là **nhỏ nhất có thể**.

---
Quy trình tổng quát (Pipeline) của Linear Regression:
<p align="center">
  <img src="{{ '/assets/module6-week1/pipeline.png' | relative_url }}" alt="Pipeline Diagram" width="600">
</p> 

**Hạn chế** của Linear Regression
- Không giới hạn đầu ra [0,1]
- Không phù hợp với ranh giới phân loại phi tuyến
- Hàm mất mát (MSE) không phù hợp đối với các bài toán phân loại
- Không đảm bảo mô hình hội tụ ổn định

# Logistic Regression
**Logistic Regression** là một mô hình **học có giám sát (supervised learning)** dùng cho bài toán **phân loại nhị phân (binary classification)**

**Mục tiêu** dự đoán **xác suất một mẫu thuộc về một lớp cụ thể** (thường là lớp “1”) trong bài toán phân loại nhị phân.

Quy trình tổng quát (Pipeline) của Logistic Regression:
<p align="center">
  <img src="{{ 'assets/module6-week1/LogisticR_training.png' | absolute_url }}"
       alt="Logistic RegressionE" width="800">
</p>

**Hạn chế** của Logistic Regression
- Dễ bị overfitting khi số lượng đặc trưng (features) lớn hơn số lượng quan sát (samples)
- Chỉ dùng cho biến phụ thuộc rời rạc (Discrete output)
- Không xử lý tốt các bài toán phi tuyến (Non-linear)
- Nhạy cảm với đa cộng tuyến (Multicollinearity)
- Khó nắm bắt mối quan hệ phức tạp giữa các biến

**Giải pháp** đối với các bài toán **phân loại nhiều lớp (multiclass classification)** là **Softmax Regression** hay còn có tên gọi khác là **Multinomial Logistic Regression (Hồi quy Logistic đa thức)**.

## Phần 2 - Softmax Regression
**1. Khái niệm**

**Softmax Regression** hay có tên gọi khác là **Multinomial Logistic Regression** là dạng tổng quát của Logistic Regression dùng đối với bài toán **phân loại nhiều lớp (multiclass classification)**. Quá trình tính toán của hàm này được chia thành 2 bước chính:

**Bước 1: Tính toán K điểm số tuyến tính**

Tương tự như Linear và Logistic Regression, ta cũng bắt đầu với mô hình tuyến tính cho các đặc trưng đầu vào nhưng ta sẽ tính cho K lớp:

$$
z_k = w^T_j*x + b_k (k = 1,2,...,K) \tag{1}
$$

Trong đó:
- $z_k$: điểm số (logit) của lớp k
- $w_k$: vector trọng số (weight vector) tương ứng với lớp k
- $x$: vector đặc trưng (feature vector) của mẫu đầu vào.
- $b_k$: hệ số bias của lớp k
- $K$: tổng số lớp 

**Bước 2: Chuyển đổi K điểm số thành K xác suất**

Hàm Softmax nhận đầu vào vector $z = \lbrace z_1, z_2, \ldots, z_k \rbrace$
và trả về vector xác suất $\hat{y} = \lbrace \hat{y}_1, \hat{y}_2, \ldots, \hat{y}_k \rbrace$ sao cho $\sum{\hat{y_k}} = 1$

**Hàm Softmax**

$$
\hat{y_k} = P(y = k | x) = \frac{e^{z_k}}{\sum_{j=1}^{K} e^{z_j}} \tag{2}
$$

Trong đó:

- $\hat{y_k}$: xác suất dự đoán mẫu thuộc lớp *k*.  
- $P(y = k \| x)$: ký hiệu xác suất có điều kiện của lớp k khi biết đầu vào x.  
- $e^{z_k}$: hàm mũ nhằm làm nổi bật sự khác biệt giữa các điểm số.  
- $\sum_{j=1}^{K} e^{z_j}$: tổng các giá trị mũ của tất cả các lớp → dùng để chuẩn hóa sao cho tổng xác suất = 1.

Tuy nhiên, hàm $e^x$ tăng rất nhanh và sẽ bị overflow.

<p align="center">
  <img src="{{ '/assets/module6-week2/exponential_graph.png' | relative_url }}" alt="Exponential function" width="600">
</p>

Giải pháp là ta đặt $m = max(z)$ và sau đó hàm softmax sẽ biến đổi như sau:

$$
\hat{y_k} = P(y = k | x) = \frac{e^{z_k - m}}{\sum_{j=1}^{K} e^{z_j - m}} \tag{3}
$$

<p align="center">
  <img src="{{ '/assets/module6-week2/exponential_graph_2.png' | relative_url }}" alt="Justifying exponential function" width="600">
</p>

Ý nghĩa:
- **Chuẩn hóa**: đảm bảo tổng các xác suất đầu ra luôn bằng 1

$$
\sum_{k=1}^{K} \hat{y_k}
= \sum_{k=1}^{K} \frac{e^{z_k}}{\sum_{j=1}^{K} e^{z_j}}
= \frac{1}{\sum_{j=1}^{K} e^{z_j}} \sum_{k=1}^{K} e^{z_k}
= 1 \tag{4}
$$

- **Làm mềm (Softmax)**: Lớp có điểm $z_k$ cao sẽ trả ra xác suất $\hat{y_k}$ vượt trội hơn. Tên gọi “Softmax” thể
hiện rằng đây là một phiên bản “mềm” và khả vi của hàm 'argmax' (một hàm 'max' thông thường vốn chỉ chọn ra một giá trị lớn nhất duy nhất và trả về 1 cho lớp đó, 0 cho các lớp
còn lại)

## Phần 3: Hàm mất mát: MSE, BCE, hay CCE?

**1. MSE (Mean Squared Error)**

Hàm MSE nếu áp dụng cho Softmax Regression:

$$
L_{MSE} = \frac{1}{2} \sum_j (\hat{y_j} - y_j)^2
$$

Đạo hàm của hàm mất mát theo logit $\( z_j \)$:

$$
\frac{\partial L_{MSE}}{\partial z_j} = \sum_k (\hat{y_k} - y_k) \frac{\partial \hat{y_k}}{\partial z_j}
$$

với **Jacobian của softmax** được cho bởi:

$$
\frac{\partial \hat{y_k}}{\partial z_j} = \hat{y_k}(\delta_{kj} - \hat{y_j})
$$

Suy ra:

$$
\frac{\partial L_{MSE}}{\partial z_j}
= (\hat{y_j} - y_j)\hat{y_j}(1 - \hat{y_j})
- \sum_{k \ne j} (\hat{y_k} - y_k)\hat{y_k}\hat{y_j}
$$

 **Gradient MSE** bị nhân thêm các hệ số $\hat{y}(1 - \hat{y})$⟹ bị **triệt tiêu** khi softmax bão hòa ($\hat{y} \to 0$ hoặc $\hat{y} \to 1$).

 **2. BCE (Binary Cross Entropy)**

Ta cùng nhìn lại công thức của hàm Softmax:

$$
\hat{y_k} = P(y = k | x) = \frac{e^{z_k}}{\sum_{j=1}^{K} e^{z_j}}
$$

Suy ra là tổng xác suất các mẫu $\sum_k{\hat{y_k}} = 1$. Hay nói cách khác là các mẫu đều có sự phụ thuộc nhất định

Tuy nhiên đối với BCE lại giả vờ rằng:

$$
P(y_1, \ldots, y_K \mid x) = \prod_i P(y_i \mid x)
$$

tức là độc lập có điều kiện. Vậy BCE không phù hợp để áp dụng chung với hàm Softmax.

Giải pháp là ta sử dụng hàm mất mát Categorical Cross-Entropy.

**3. Categorical Cross Entropy (CCE)**

**Categorical Cross Entropy** đo lường **sự khác biệt** giữa **phân phối xác suất dự đoán** và **phân phối thực tế** (đúng) của các lớp. Công thức này được áp dụng đối với bài toán **phân loại nhiều lớp (multi-class classification)**

Dạng công thức tổng quát:

$$
L_{\mathrm{CE}} = - \sum_{i=1}^{K} y_i \log \hat{y_i} \tag{5}
$$

Trong đó:
- **$L_{CE}$** là hàm loss *categorical cross-entropy*.

- **$y_i$** là nhãn thật (0 hoặc 1 cho mỗi lớp), lấy từ vector nhãn được mã hóa one-hot.

- **$\hat{y}_i$** là xác suất dự đoán của mô hình cho lớp $i$.

- **$C$** là số lượng lớp.

## Phần 4: Gradient Descent cho Softmax Regression ##

Sau khi tính toán loss thông qua Categorical Cross Entropy, bước tiếp theo ta sẽ tối ưu hóa tham số là trọng số W và hệ số bias b.

Phương pháp phổ biến nhất là Gradient Descent, cập nhật tham số ngược lại với gradient của hàm mất mát.

Để áp dụng Gradient Descent, chúng ta cần tính đạo hàm riêng (gradient) của L theo W và b. Quá trình này bắt buộc phải dùng quy tắc chuỗi (chain rule):

$$
\frac{\partial L_i}{\partial z_{ik}}
= 
\sum_{j=1}^{K}
\frac{\partial L_i}{\partial \hat{y}_{ij}}
\frac{\partial \hat{y}_{ij}}{\partial z_{ik}}. \tag{6}
$$

**Bước 1: Đạo hàm của Loss theo Điểm số (Logits)**

**Thành phần 1 $\frac{\partial L_i}{\partial \hat{y}_{ij}}$ (Đạo hàm của Loss theo dự đoán)**

$$
L_i = - \sum_{j=1}^{K} y_{ij} \log(\hat{y}_{ij}).
$$

Khi lấy đạo hàm $L_i$ theo một $\hat{y}_{ij}$ cụ thể, chỉ có thành phần j trong tổng là còn lại

$$
\frac{\partial L_i}{\partial \hat{y}_{ij}}
= 
\frac{\partial}{\partial \hat{y}_{ij}}
\left( -y_{ij} \log(\hat{y}_{ij}) \right)
= 
- y_{ij} \frac{1}{\hat{y}_{ij}}. \tag{7}
$$

**Thành phần 2: $ \frac{\partial \hat{y_{ij}}}{\partial z_{ik}} $ (Đạo hàm của hàm Softmax)**

Có 2 trường hợp:

**Trường hợp 1**: j = k (Đạo hàm của $\hat{y_{ij}}$ theo $z_{ij}$)

$$
\hat{y}_{ij} = \frac{e^{z_{ij}}}{\sum_{l=1}^{K} e^{z_{il}}}
$$

Sử dụng quy tắc đạo hàm thương (quotient rule) $ \left( \frac{u}{v} \right)' = \frac{u'v - uv'}{v^2} $:

$$
\frac{\partial \hat{y}_{ij}}{\partial z_{ij}}
=
\frac{
(e^{z_{ij}})' \left( \sum_{l} e^{z_{il}} \right)
-
e^{z_{ij}} \left( \sum_{l} e^{z_{il}} \right)'
}
{\left( \sum_{l} e^{z_{il}} \right)^2}
$$

$$
= \frac{e^{z_{ij}}(\sum{e^{il}}) - e^{z_{ij}}(e^{z_{ij}}) } {(\sum{e^{z_{il}}})^2} = \frac{e^{z_{ij}}}{\sum{e^{z_{il}}}}\frac{(\sum{e^{z_{il}}})- e^{z_{ij}}}{(\sum{e^{z_{il}}})}
$$

$$
= (\frac{e^{z_{ij}}}{\sum{e^{z_{il}}}})(1-\frac{e^{z_{ij}}}{(\sum{e^{z_{il}}})}) = \hat{y_{ij}}(1-\hat{y_{ij}}) \tag{8}
$$

**Trường hợp 2: $j \ne k$ (Đạo hàm của $\hat{y_{ij}}$ theo $z_{ik}$)**  
Tương tự, sử dụng quy tắc thương:

$$
\frac{\partial \hat{y}_{ij}}{\partial z_{ik}}
=
\frac{
(e^{z_{ij}})' \left( \sum_{l} e^{z_{il}} \right)
-
e^{z_{ij}} \left( \sum_{l} e^{z_{il}} \right)'
}
{
\left( \sum_{l} e^{z_{il}} \right)^2
}
$$

Lúc này,  
- $(e^{z_{ij}})' = 0$ vì đạo hàm theo $z_{ik}$ nhưng $j \ne k$  
- $\left( \sum_{l} e^{z_{il}} \right)' = e^{z_{ik}}$

Do đó:

$$
\frac{0(\sum{e^{z_{il}}}) - e^{z_{ij}}(e^{z_ik})}{(\sum{e^{z_{il}}})^2} = -(\frac{e^{z_{ij}}}{\sum{e^{z_{il}}}})(\frac{e^{z_{ik}}}{\sum{e^{z_{il}}}})
= -\hat{y_{ij}}\hat{y_{ik}} \tag{9}
$$

Kết hợp lại, ta có:

$$
\frac{\partial L_i}{\partial z_{ik}}
= 
\sum_{j=1}^{K}
\frac{\partial L_i}{\partial \hat{y}_{ij}}
\frac{\partial \hat{y}_{ij}}{\partial z_{ik}}.
$$

Chúng ta tách tổng này ra thành 2 phần: phần $j = k$ và phần $j \neq k$:

$$
\frac{\partial L_i}{\partial z_{ik}}
=
\underbrace{
\left(
\frac{\partial L_i}{\partial \hat{y}_{ik}}
\frac{\partial \hat{y}_{ik}}{\partial z_{ik}}
\right)
}_{j = k}
\;+\;
\underbrace{
\sum_{j \ne k}
\left(
\frac{\partial L_i}{\partial \hat{y}_{ij}}
\frac{\partial \hat{y}_{ij}}{\partial z_{ik}}
\right)
}_{j \ne k}.
$$

Thế công thức $(7), (8), (9)$ vào, ta có:

Thay các kết quả ta vừa tìm được vào:

$$
= ((-\frac{y_{ik}}{\hat{y_{ik}}})(\hat{y_{ik}}(1-\hat{y_{ik}}))) + \sum_{j \ne k}((-\frac{y_{ij}}{\hat{y_{ij}}})(-\hat{y_{ij}}\hat{y_{ik}}))
$$

$$
= (-y_{ik}(1-\hat{y_{ik}})) + \sum_{j \ne k} (y_{ij}\hat{y_{ik}})
$$

$$
= (-y_{ik}+y_{ik}\hat{y_{ik}}) + \sum_{j \ne k} (y_{ij}\hat{y_{ik}})
$$

$$
= -y_{ik} + \hat{y_{ik}}(y_{ik} + \sum_{j \ne k} (y_{ij}))
$$

**Điểm mấu chốt:** Vì $y_i$ là vector one-hot, nên tổng tất cả các phần tử của nó $\sum_{j=1}^{K} y_{ij} = 1$.  

Do đó:

$$
\left( y_{ik} + \sum_{j \ne k} y_{ij} \right) = 1.
$$

Phương trình trở thành:

$$
\frac{\partial L_i}{\partial z_{ik}}
=
- y_{ik} + \hat{y}_{ik}(1)
=
\hat{y}_{ik} - y_{ik}.
\tag{7}
$$

Vậy kết quả bước 1 đạo hàm theo Logits là:

$$
\frac{\partial L_i}{\partial z_i}
=
\hat{y}_{i} - y_{i} = e_i \tag{8}
$$

Đây chính là **vector lỗi (error vector)** $e_i$, là sự chênh lệch trực tiếp giữa dự đoán và nhãn thực tế.

**Bước 2: Đạo hàm theo tham số (W và b)**

**Mục tiêu A: Tìm  $\dfrac{\partial L}{\partial b}$**

Áp dụng quy tắc Chain Rule, ta có.

$$
\frac{\partial L_i}{\partial b_k}
=
\frac{\partial L_i}{\partial z_{ik}}
\frac{\partial z_{ik}}{\partial b_k}.
$$

---

- Từ bước 1, ta có: $$\frac{\partial L_i}{\partial z_{ik}} = \hat{y}_{ik} - y_{ik} = e_{ik}$$

- Ta có: $$z_{ik} = (w_k^T x_i) + b_k.$$ => $$\frac{\partial z_{ik}}{\partial b_k} = 1.$$

Suy ra:

$$
\frac{\partial L_i}{\partial b_k}
=
e_{ik} \times 1
=
e_{ik}.
$$

---

Lấy trung bình trên $N$ mẫu và viết ở dạng vector:

$$
\frac{\partial L}{\partial b}
=
\frac{1}{N}
\sum_{i=1}^{N}
\frac{\partial L_i}{\partial b}
=
\frac{1}{N}
\sum_{i=1}^{N}
e_i
=
\frac{1}{N}
\sum_{i=1}^{N}
(\hat{y}_i - y_i). \tag{9}
$$

**Mục tiêu B: Tìm  $\dfrac{\partial L}{\partial W}$**

Áp dụng quy tắc Chain Rule, ta có

$$
\frac{\partial L_i}{\partial w_{kd}}
=
\sum_{j=1}^{K}
\frac{\partial L_i}{\partial z_{ij}}
\frac{\partial z_{ij}}{\partial w_{kd}}.
\tag{10}
$$

Tuy nhiên, $w_{kd}$ (trọng số của lớp $k$) chỉ ảnh hưởng đến $z_{ik}$ (điểm số của lớp $k$). Do đó, $\frac{\partial z_{ij}}{\partial w_{kd}} = 0$ với mọi $j \ne k$.  

Tổng $\sum$ được rút gọn:

$$
\frac{\partial L_i}{\partial w_{kd}}
=
\frac{\partial L_i}{\partial z_{ik}}
\frac{\partial z_{ik}}{\partial w_{kd}}.
\tag{11}
$$

---

- Ta đã có: $$ \frac{\partial L_i}{\partial z_{ik}} = \hat{y}_{ik} - y_{ik} = e_{ik} $$ (ở công thức số $(8)$ )

- Ta có: $$ z_{ik} = \sum_{d'=1}^{D} w_{kd'} x_{id'} + b_k$$

- Lấy đạo hàm $z_{ik}$ theo $w_{kd}$:
  $$
  \frac{\partial z_{ik}}{\partial w_{kd}} = x_{id}.
  $$

Suy ra:

$$
\frac{\partial L_i}{\partial w_{kd}}
=
e_{ik} \cdot x_{id}.
\tag{12}
$$

---

Đây là đạo hàm cho **một phần tử** $(k, d)$ của ma trận $W$.  
Vậy:

$$
\frac{\partial L_i}{\partial W}
=
e_i x_i^T
=
(\hat{y}_i - y_i) x_i^T.
\tag{13}
$$

---

Lấy trung bình trên $N$ mẫu:

$$
\frac{\partial L}{\partial W}
=
\frac{1}{N}
\sum_{i=1}^{N}
\frac{\partial L_i}{\partial W}
=
\frac{1}{N}
\sum_{i=1}^{N}
(\hat{y}_i - y_i) x_i^T.
\tag{14}
$$


**Công thức Gradient**

Sau khi rút gọn các kết quả phức tạp trên, ta thu được công thức sau:

$$
\nabla_W L
=
\frac{\partial L}{\partial W}
=
\frac{1}{N}
\sum_{i=1}^{N}
(\hat{y}_{i} - y_{i}) x_i^T
$$

$$
\nabla_b L
=
\frac{\partial L}{\partial b}
=
\frac{1}{N}
\sum_{i=1}^{N}
(\hat{y}_{i} - y_{i})
$$

---
Hay có thể hiểu theo cách đơn giản sau:

- $\nabla_{\text{weights}} L = \dfrac{1}{N} \sum (\text{dự đoán} - \text{thực tế}) \cdot (\text{đầu vào})^T$

- $\nabla_{\text{bias}} L = \dfrac{1}{N} \sum (\text{dự đoán} - \text{thực tế})$



 









## Phần 5: Quy trình huấn luyện ##

Quá trình huấn luyện cho Softmax Regression khá giống với Linear và Logistic Regression. Quá trình này cũng sử dụng Batch Gradient Descent để tối ưu hóa các tham số là ma trận W và vector hệ số bias b

<p align="center">
  <img src="{{ '/assets/module6-week2/Pipeline_Softmax_Regression.png' | relative_url }}" alt="Pipeline Diagram" width="600">
</p>













