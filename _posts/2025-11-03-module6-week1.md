---
layout: post
title: Module 6 – Tuần 1
date: 06-11-2025
categories: AIO2025 Module6 Deep Learning
use_math:  true
image: /assets/module6-week1/m6w1.jpg
---

{% include mathjax.html %}


## Tóm tắt bài giảng ngày thứ Tư ngày 05/11/2025  

<p align="center">
  <img src="{{ '/assets/module6-week1/m6w1.jpg' | relative_url }}" alt="Logistic Regression vs Linear Regression illustration" width="80%">
</p>



## 📈 Phần 1 – Ôn lại Linear Regression

**Linear Regression (Hồi quy tuyến tính)** là một mô hình dùng để **dự đoán giá trị liên tục** dựa trên các biến đầu vào (*features*).  
Mục tiêu của mô hình là tìm một đường thẳng (hoặc siêu phẳng trong không gian nhiều chiều) sao cho tổng sai số giữa dự đoán và dữ liệu thật là nhỏ nhất.

Phương trình tổng quát:
 

$$
\hat{y} = w_1x_1 + w_2x_2 + \dots + w_nx_n + b = \mathbf{X}\boldsymbol{\theta}
$$


Trong đó:

- $\mathbf{X}\in\mathbb{R}^{m\times n}$: ma trận dữ liệu đầu vào gồm **m mẫu** và **n đặc trưng**
  (mỗi hàng là 1 mẫu dữ liệu, mỗi cột là 1 đặc trưng)
- $\boldsymbol{\theta}=[w_1, w_2, \ldots, w_n, b]^T$: vector trọng số và bias
- $\hat{y}$: giá trị dự đoán đầu ra
- $y$: giá trị thực tế của dữ liệu huấn luyện
- $b$: hệ số bias giúp điều chỉnh vị trí đường hồi quy
 


---  

$$
L(\hat{y}, y) = \frac{1}{m} \sum_{i=1}^{m} (\hat{y}_i - y_i)^2
$$

Trong đó:

- $y_i$: giá trị thật  
- $\hat{y}_i$: giá trị dự đoán  
- $m$: số lượng mẫu  


---

Mục tiêu của mô hình **Linear Regression** là **tìm ra các tham số tối ưu** — cụ thể là **vector trọng số** $\mathbf{w}$ và **hệ số bias** $b$ — sao cho giá trị của $L_{\text{MSE}}$ là **nhỏ nhất có thể**.


<p align="center">
  <img src="{{ '/assets/module6-week1/loss-function.png' | relative_url }}" alt="Loss Function" width="600">
</p>  

- Mỗi cặp $(\hat{y}^{(i)}, y^{(i)})$ thể hiện **mức độ sai lệch** giữa dự đoán và giá trị thực.  
- Bình phương sai số $(\hat{y}^{(i)} - y^{(i)})^2$ giúp **loại bỏ dấu âm** và **phạt mạnh hơn các dự đoán sai lệch lớn**.  
- Sau khi lấy trung bình trên tất cả các mẫu, ta thu được **mức sai số trung bình toàn bộ mô hình**.  
- Bằng cách điều chỉnh $\mathbf{w}$ và $b$, mô hình “xoay” hoặc “dịch chuyển” đường hồi quy để **làm giảm sai số tổng thể** này.\

---

**Quy trình tổng quát (Pipeline) của Linear Regression**

Sau khi hiểu cách mô hình học để giảm sai số (loss), ta có thể khái quát quy trình huấn luyện Logistic Regression qua các bước sau:

![Pipeline Diagram]({{ "/assets/module6-week1/pipeline.png" | relative_url }})


---

**⚠️ Hạn chế của Linear Regression trong bài toán phân loại**

Mặc dù Linear Regression là mô hình nền tảng, dễ hiểu và hiệu quả trong các bài toán **dự đoán giá trị liên tục**,  
nhưng khi áp dụng cho **bài toán phân loại nhị phân (0/1)**, nó bộc lộ một số **nhược điểm nghiêm trọng**:

**Không giới hạn đầu ra trong [0, 1]**
- Mô hình tuyến tính có dạng $\hat{y} = \mathbf{w}^T\mathbf{x} + b$.  
- Đầu ra $\hat{y}$ có thể nhận **bất kỳ giá trị nào** trong khoảng $(-\infty, +\infty)$.  
> Điều này **không thể diễn giải như một xác suất**, và khiến việc **đặt ngưỡng phân loại (threshold)** trở nên tùy tiện.

**Mô hình tuyến tính không phù hợp với ranh giới phân loại phi tuyến**
- Trong nhiều bài toán thực tế, dữ liệu của hai lớp (0 và 1) **không thể phân tách bằng một đường thẳng**.  
- Linear Regression chỉ học được mối quan hệ tuyến tính giữa đầu vào và đầu ra, nên **không thể mô hình hóa ranh giới phi tuyến**.

**Hàm mất mát (MSE) không hiệu quả cho phân loại**
- Linear Regression sử dụng **Mean Squared Error (MSE)**, vốn được thiết kế cho bài toán hồi quy.  
- Trong phân loại, hàm này:
  - Không phản ánh tốt **mức độ tin cậy** của dự đoán xác suất.  
  - Khi kết hợp với hàm sigmoid, MSE khiến **gradient hội tụ chậm** và **dễ mắc kẹt tại cực trị cục bộ**.

**Không đảm bảo mô hình hội tụ ổn định**
- Vì đầu ra $\hat{y}$ không bị giới hạn, mô hình có thể **tăng/giảm vô hạn** để cố gắng giảm MSE.  
- Kết quả là:
  - Gradient descent **dao động hoặc diverge** (không hội tụ).  
  - Mô hình **quá nhạy cảm với nhiễu hoặc ngoại lệ (outliers)**.

👉 Giải pháp là **Logistic Regression**, một mô hình mở rộng Linear Regression với **hàm kích hoạt sigmoid** để ánh xạ đầu ra về khoảng (0, 1) — giúp mô hình **dự đoán xác suất chính xác hơn**.


## 🔀 Phần 2 – Logistic Regression

**1. Khái niệm cơ bản**

**Logistic Regression** là một mô hình **học có giám sát (supervised learning)** dùng cho **bài toán phân loại nhị phân (binary classification)** 

⚖️ So sánh nhanh:

| Mô hình              | Dạng đầu ra                     | Ví dụ                          |
|----------------------|----------------------------------|---------------------------------|
| **Linear Regression**  | Liên tục (*continuous*)          | Dự đoán nhiệt độ, giá nhà       |
| **Logistic Regression**| Rời rạc (*discrete*, 0 hoặc 1)   | Dự đoán có bệnh hay không       |


**2. Bắt đầu từ mô hình tuyến tính**

Tương tự như **Linear Regression**, **Logistic Regression** cũng bắt đầu với **một mô hình tuyến tính** để kết hợp các đặc trưng đầu vào:

$$
z = wx + b
$$

Trong đó:

- $x$: giá trị đặc trưng đầu vào (*feature*)  
- $w$: trọng số (*weight*) mà mô hình cần học  
- $b$: hệ số chệch (*bias*)  
- $z$: giá trị tuyến tính (*logit*)

Ở bước này, $z$ có thể nhận mọi giá trị thực từ $-\infty$ đến $+\infty$.  
Tuy nhiên, mục tiêu của ta là **dự đoán xác suất** một mẫu thuộc lớp 1 (ví dụ: “có bệnh”, “đỗ”, “spam”…), nên đầu ra cần nằm trong khoảng **(0, 1)**.

👉 Để làm được điều đó, ta **không dùng trực tiếp** $z$ làm kết quả dự đoán mà cần một **hàm chuyển đổi** phù hợp.

**3. Hàm Sigmoid**

Nếu ta dùng luôn công thức:

$$
\hat{y} = z = \mathbf{w}x + b
$$

để dự đoán cho bài toán **phân loại** (ví dụ “có bệnh” hay “không bệnh”), ta gặp ngay vấn đề:

- $z$ có thể nhận **bất kỳ giá trị nào** từ $-\infty$ đến $+\infty$
- Nhưng **xác suất thực tế** phải nằm trong $[0, 1]$

Chính vì thế, **hàm sigmoid** (hay *logistic function*) xuất hiện để biến đổi giá trị tuyến tính \(z\) thành một **xác suất hợp lệ**:

$$
\sigma(z) = \frac{1}{1 + e^{-z}}
$$

<p align="center">
  <img src="{{ '/assets/module6-week1/sigmoid-function.png' | relative_url }}" alt="Sigmoid Function" width="500">
</p>


**🧠 Giải thích:**

- Khi $z \to +\infty \Rightarrow e^{-z} \to 0 \Rightarrow \sigma(z) \to 1$  
- Khi $z = 0 \Rightarrow \sigma(0) = \frac{1}{2} = 0.5$  
- Khi $z \to -\infty \Rightarrow e^{-z} \to +\infty \Rightarrow \sigma(z) \to 0$

Vì vậy, **sigmoid nén toàn bộ giá trị thực của $z$ vào khoảng (0, 1)** — chính xác là miền giá trị cần thiết để mô tả xác suất.

## 🎯 Phần 3 – Hàm mất mát: MSE hay BCE?

**1. Thử dùng MSE (Mean Squared Error)**

Đây là **hàm mất mát quen thuộc** của hồi quy tuyến tính:

$$
L_{\text{MSE}}(\hat{y}, y) = (\hat{y} - y)^2
$$

Khi áp dụng cho **Logistic Regression**:

$$
\hat{y} = \sigma(w x + b) = \frac{1}{1 + e^{-(w x + b)}}
$$

→ Ta có thể tính được đạo hàm và cập nhật tham số bằng **Gradient Descent**.

**⚠️ Tuy nhiên, MSE *không phù hợp* cho Logistic Regression vì:**

**Hàm sigmoid phi tuyến → độ dốc phức tạp**

- Kết hợp **sigmoid + MSE** làm đồ thị hàm mất mát trở nên **không lồi (non-convex)**.  
- **Gradient** có thể bị rất nhỏ (*hiện tượng vanishing gradient*) khi $\hat{y}$ tiến gần 0 hoặc 1.  
  → Mô hình học **rất chậm**, dễ **mắc kẹt ở cực tiểu cục bộ**.

<p align="center">
  <img src="{{ 'assets/module6-week1/MSE-Sigmoid.png' | absolute_url }}"
       alt="Kết hợp Sigmoid và MSE" width="500">
</p>

👉 Chính vì sự nguyên hiểm này, một hàm mất mát đã được thiết kế riêng cho bài toán phân loại xác suất, đó là Binary Cross-Entropy (BCE), hay còn gọi là Log Loss.

<p align="center">
  <img src="{{ 'assets/module6-week1/BCE-Sigmoid.png' | absolute_url }}"
       alt="Kết hợp Sigmoid và BCE" width="500">
</p>


**2. BCE (Binary Cross-Entropy)**

**a) Công thức tổng quát**

$$
L_{\text{BCE}}(\hat{y}, y) = -\Big[\, y \log(\hat{y}) + (1 - y)\, \log(1 - \hat{y}) \,\Big]
$$

**b) Giải thích chi tiết các thành phần trong công thức**

| Ký hiệu | Ý nghĩa | Vai trò |
|----------|----------|----------|
| $y$ | **Giá trị thật (label)** của mẫu dữ liệu, chỉ nhận hai giá trị: 0 hoặc 1. | Cho biết mẫu thuộc lớp nào (lớp dương = 1, lớp âm = 0). |
| $\hat{y}$ | **Xác suất dự đoán** mà mô hình logistic regression sinh ra, được tính bằng hàm sigmoid:  $\hat{y} = \frac{1}{1 + e^{-(wx + b)}}$ | Biểu diễn độ tin cậy của mô hình rằng mẫu này thuộc lớp 1. |
| $\log(\hat{y})$ | Logarithm tự nhiên của xác suất dự đoán lớp 1 | Dùng để đo “**mức độ bất ngờ**” (*information content*) khi mô hình dự đoán đúng lớp 1. |
| $\log(1 - \hat{y})$ | Logarithm tự nhiên của xác suất dự đoán lớp 0 | Đo “**mức độ bất ngờ**” khi mô hình dự đoán đúng lớp 0. |
| Dấu trừ (-) bên ngoài | Đảo ngược dấu vì log-likelihood có giá trị âm | Giúp hàm loss trở thành giá trị dương cần **minimize**. |

**c) Ý nghĩa trực quan**

Công thức trên có thể hiểu là:

- **Nếu mẫu thật \( y = 1 \)**, còn lại:

$$
L = -\log(\hat{y})
$$

→ Mô hình sẽ bị **phạt mạnh** khi \(\hat{y}\) nhỏ (vì \(\log(\hat{y})\) rất âm khi \(\hat{y}\) gần 0).  
→ Điều này **khuyến khích mô hình đẩy \(\hat{y}\) gần 1\)** cho mẫu thuộc lớp 1.

---

- **Nếu mẫu thật \( y = 0 \)**, còn lại:

$$
L = -\log(1 - \hat{y})
$$

→ Mô hình bị **phạt mạnh** khi \(\hat{y}\) lớn (tức dự đoán lớp 1 sai).  
→ **Khuyến khích mô hình đẩy \(\hat{y}\) gần 0\)** cho mẫu thuộc lớp 0.

---



![BCE + Sigmoid]({{ '/assets/module6-week1/bce-sigmoid.png' | relative_url }})

## 🛠️ Phần 4 – Học tham số bằng Gradient Descent

Với 1 mẫu $(\mathbf{x}, y)$, đạo hàm theo $\mathbf{w}$ của BCE:
$$
\nabla_{\mathbf{w}} L = (\hat{p}-y)\,\mathbf{x},\qquad
\frac{\partial L}{\partial b} = \hat{p}-y
$$
Cập nhật:
$$
\mathbf{w} \leftarrow \mathbf{w} - \eta \, (\hat{p}-y)\mathbf{x},\qquad
b \leftarrow b - \eta \, (\hat{p}-y)
$$
($\eta$: learning rate)

---

## 🧭 Phần 5 – Decision Boundary & Prob. View

- Biên quyết định khi $\hat{p}=0.5 \iff \mathbf{w}^\top \mathbf{x}+b=0$ ⇒ là **siêu phẳng tuyến tính**.
- Nhìn theo **log-odds**:
$$
\log\frac{\hat{p}}{1-\hat{p}} = \mathbf{w}^\top \mathbf{x} + b
$$
→ Logistic Regression chính là **hồi quy tuyến tính trên log-odds**.

---

## 📚 Tài nguyên
- [Coding cho bài giảng của thầy Quang-Vinh Dinh (Google Drive)](https://…)  
- [Wikipedia – Logistic Function](https://en.wikipedia.org/wiki/Logistic_function)

---

## 📘 Tài Liệu học mở rộng – Logistic Regression & Vectorization

<div style="border:1px solid #dcdcdc; border-radius:12px; padding:20px; background:#fdfdfd; box-shadow:2px 2px 8px rgba(0,0,0,0.05);">
  <h4 style="margin-top:0;">📄 Report MD06W01_v1.pdf</h4>
  <p style="margin:6px 0;">
    Bản tóm tắt 4 trang A4 nội dung tuần 1 – <b>Logistic Regression</b>, 
    <i>Vectorization and Application</i>.
  </p>
  <p style="margin:6px 0;">
    <a href="{{ site.baseurl }}/assets/module6-week1/Report_MD06W01_v1.pdf" 
       target="_blank" rel="noopener"
       style="background:#e6f0ff; padding:8px 16px; border-radius:8px; text-decoration:none; font-weight:600; color:#0044cc;">
      🔗 Xem hoặc tải PDF
    </a>
  </p>
</div>

---


## 🧩 Mini Quiz (vui học 🎲)  
1️⃣ Logistic Regression dự đoán **cái gì**? → **Xác suất** $P(y=1\mid \mathbf{x})$ trong $[0,1]$.  

2️⃣ Vì sao chọn **BCE** thay vì **MSE**? → Phù hợp xác suất + gradient “khỏe” hơn.  

3️⃣ Khi nào $\hat{y}=1$? → Khi $\sigma(\mathbf{w}^\top\mathbf{x}+b)\ge0.5$ (tương đương $\mathbf{w}^\top\mathbf{x}+b\ge0$). 

4️⃣ Sigmoid function đảm bảo điều gì?  
   ☑ Đầu ra luôn trong [0,1]  
   ☐ Tốc độ học nhanh hơn  

---

💡 *Hẹn gặp ở bài giảng tiếp theo của tuần 1 – Khi chúng ta dive sâu hơn vào Multi-feature Logistic Regression nhé!* 🚀


---

---
layout: post
title: "💜 Advanced Logistic Regression — Vectorization & Application (AIO 2025)"
date: 2025-11-08
---

## Tóm tắt bài giảng ngày thứ Sáu ngày 07/11/2025

## 🌸 Giới thiệu

Tuần này, chúng ta bước sang **Logistic Regression nâng cao**, với hai trọng tâm:
1. Làm chủ **vectorization** – biến toàn bộ phép tính thành dạng ma trận để tăng tốc huấn luyện.
2. Hiểu sâu **gradient descent & binary cross-entropy loss** qua góc nhìn đại số tuyến tính.

Mục tiêu cuối cùng: viết lại **toàn bộ Logistic Regression pipeline** dưới dạng **vector hóa** — nhanh hơn, ngắn gọn hơn, và dễ mở rộng sang neural network sau này. 💻

---

## 🌿 Phần 1 – Logistic Regression cơ bản

### 💎 Bản chất mô hình

Mặc dù tên gọi là **"Regression"**, nhưng Logistic Regression thực chất là **một mô hình phân loại nhị phân (binary classification)**, không phải hồi quy tuyến tính thông thường.  
Thay vì dự đoán giá trị liên tục, ta dự đoán **xác suất đầu ra thuộc lớp 1**, ký hiệu:

$$
P(y=1|x) = \hat{y} = \sigma(z)
$$

với:
$$
z = w^T x + b
$$

Trong đó:  
- $w$ là vector trọng số (weights)  
- $b$ là hệ số chệch (bias)  
- $x$ là vector đặc trưng đầu vào (feature vector)  
- $\sigma(z)$ là **hàm sigmoid**, giúp ánh xạ mọi giá trị thực về khoảng (0,1), phù hợp để diễn giải như xác suất.

---

### 🧭 Hàm sigmoid – Cửa ngõ giữa tuyến tính và xác suất

Hàm sigmoid được định nghĩa:

$$
\sigma(z) = \frac{1}{1 + e^{-z}}
$$

Đặc tính:
- Khi $z \to +\infty \Rightarrow \sigma(z) \to 1$
- Khi $z \to -\infty \Rightarrow \sigma(z) \to 0$
- Tại $z = 0 \Rightarrow \sigma(0) = 0.5$

→ Đây là **điểm trung gian** biểu thị ranh giới quyết định giữa hai lớp (0 và 1).

Hàm sigmoid giúp mô hình **chuyển hóa đầu ra tuyến tính thành xác suất** – một bước quan trọng trong mọi mô hình phân loại nhị phân.

---

### 📈 Quy tắc phân loại

Dựa trên xác suất \(\hat{y}\) mà mô hình dự đoán, ta có quy tắc:

- Nếu $\hat{y} \ge 0.5 \Rightarrow y_{\text{pred}} = 1$
- Nếu $\hat{y} < 0.5 \Rightarrow y_{\text{pred}} = 0$

Ví dụ:  
Nếu mô hình dự đoán xác suất 0.83 → ta gán nhãn “1” (thuộc lớp dương).  
Nếu xác suất 0.24 → ta gán nhãn “0” (thuộc lớp âm).

---

### ⚙️ Hàm mất mát – Binary Cross Entropy (Log Loss)

Để huấn luyện Logistic Regression, ta cần một hàm mất mát đo độ sai lệch giữa xác suất dự đoán \(\hat{y}\) và nhãn thực \(y\):

$$
L(\hat{y}, y) = -[y \log(\hat{y}) + (1 - y)\log(1 - \hat{y})]
$$

Giải thích:
- Nếu nhãn thật $y = 1$: hàm loss chỉ giữ lại $-\log(\hat{y})$ → phạt nặng khi mô hình dự đoán xác suất thấp.  
- Nếu nhãn thật $y = 0$: hàm loss chỉ giữ lại $-\log(1 - \hat{y})$ → phạt nặng khi mô hình dự đoán xác suất cao.

Khi huấn luyện trên nhiều mẫu (N mẫu):
$$
J(w, b) = \frac{1}{N} \sum_{i=1}^{N} L(\hat{y}^{(i)}, y^{(i)})
$$

---

### 🔧 Cập nhật tham số – Gradient Descent cơ bản

Mục tiêu: tìm bộ tham số \(w, b\) sao cho hàm mất mát \(J(w,b)\) nhỏ nhất.  
Ta dùng **phương pháp Gradient Descent**, cập nhật theo hướng ngược chiều gradient:

$$
w := w - \eta \frac{\partial J}{\partial w}, \qquad b := b - \eta \frac{\partial J}{\partial b}
$$

với \( \eta \) là **learning rate** (tốc độ học).

Công thức đạo hàm riêng cho một mẫu:

$$
\frac{\partial L}{\partial w} = x(\hat{y} - y), \qquad
\frac{\partial L}{\partial b} = (\hat{y} - y)
$$

→ Đây là công thức cốt lõi để huấn luyện mô hình Logistic Regression trên từng mẫu dữ liệu.

---

### 💡 Minh họa trực quan: quy trình học với một mẫu

1. **Chọn một mẫu dữ liệu** $(x, y)$   
2. **Tính đầu ra dự đoán:**  
   $\hat{y} = \sigma(w^T x + b)$
3. **Tính mất mát:**  
   $L = -[y \log(\hat{y}) + (1-y)\log(1-\hat{y})]$  
4. **Tính gradient:**  
   $dW = x(\hat{y} - y), \quad db = (\hat{y} - y)$  
5. **Cập nhật tham số:**  
   $w := w - \eta dW, \quad b := b - \eta db$  

Quy trình này được lặp lại cho đến khi mô hình hội tụ – tức khi mất mát giảm về mức ổn định.

---

### 🧩 Ý nghĩa hình học

- Đường **decision boundary** được xác định bởi $w^T x + b = 0$.  
- Ở ranh giới này, mô hình cho xác suất $\hat{y} = 0.5$.  
- Hai phía của đường này tương ứng với hai lớp (0 và 1).  
- Mỗi lần cập nhật $w, b$, mô hình “xoay” và “tịnh tiến” ranh giới để phân chia dữ liệu tốt hơn.

---

### ⚛️ Mở rộng: mối liên hệ với hàm mất mát của Linear Regression

Linear Regression dùng **Mean Squared Error (MSE)**, còn Logistic Regression dùng **Binary Cross-Entropy (Log Loss)**.  
Tuy khác nhau về công thức, nhưng cả hai đều có cùng mục tiêu: giảm sai số dự đoán so với nhãn thật.  
Sự khác biệt chủ yếu đến từ:
- MSE giả định đầu ra là giá trị thực, còn Log Loss giả định đầu ra là xác suất.  
- MSE không phù hợp khi \(y\) là nhị phân, vì mô hình có thể dự đoán giá trị ngoài [0,1].

---

### 🌈 Tổng kết phần 1

- Logistic Regression là **mô hình phân loại xác suất nhị phân**, không phải hồi quy.  
- Sử dụng **hàm sigmoid** để chuyển đầu ra tuyến tính thành xác suất.  
- Dùng **Binary Cross-Entropy loss** để đo sai lệch.  
- Huấn luyện bằng **Gradient Descent**, cập nhật \(w, b\) qua đạo hàm riêng.  
- Có thể mở rộng sang **vectorization**, **mini-batch**, và **deep learning layers** sau này.

---

📘 *Nguồn: Advanced Logistic Regression Slides – Quang-Vinh Đinh, PhD (2025)* :contentReference[oaicite:1]{index=1}

---

## 🌸 Phần 2 – Vectorization cho mẫu đơn, mini-batch và toàn bộ tập dữ liệu

### 🌿 1️⃣ Từ 1 mẫu đơn → vector hóa

Ở phần 1 ta tính toán với một mẫu $(x, y)$ duy nhất:
$$
z = w^T x + b,\qquad 
\hat y = \sigma(z) = \frac{1}{1+e^{-z}}
\]
\[
\nabla_w L = x(\hat y - y),\quad 
\nabla_b L = \hat y - y
$$  

Vector hóa giúp gộp các phép nhân, cộng và đạo hàm vào **các phép toán ma trận** để tăng tốc và giảm sai số lập trình.  
Ta gộp \(b\) vào vector tham số \(\theta = [b, w_1,\dots,w_n]^T\) và mở rộng đặc trưng $x \to x' = [1,x_1,\dots,x_n]^T$.  

Khi đó:
$$
z = \theta^T x',\qquad 
\hat y = \sigma(z)
\]
\[
\nabla_\theta L = x'(\hat y - y)
\]
\[
\theta := \theta - \eta\nabla_\theta L
$$
Công thức này giữ nguyên cho mọi mẫu, và là hạt nhân để chuyển từ **1 mẫu → m mẫu**.

---

### 🪷 2️⃣ Vectorization cho *m-samples* (mini-batch)

Khi huấn luyện trên m mẫu đồng thời $(x^{(1)},…,x^{(m)})$, ta sắp chúng thành ma trận:
$$
X = 
\begin{bmatrix}
1 & x^{(1)}_1 & … & x^{(1)}_n \\
1 & x^{(2)}_1 & … & x^{(2)}_n \\
⋮ & ⋮ & ⋱ & ⋮ \\
1 & x^{(m)}_1 & … & x^{(m)}_n
\end{bmatrix}_{m\times(n+1)},\quad 
\theta = [b,w_1,…,w_n]^T
$$  

#### 🔹 Bước 1 – Tính đầu ra
$$
z = X \theta,\qquad 
\hat y = \sigma(z)
$$

#### 🔹 Bước 2 – Hàm mất mát toàn mini-batch
$$
L(\hat y,y)
 = -\frac1m\sum_{i=1}^m 
\big[y^{(i)}\log\hat y^{(i)}+(1-y^{(i)})\log(1-\hat y^{(i)})\big]
$$

#### 🔹 Bước 3 – Đạo hàm vector hóa
$$
\nabla_\theta L = \frac1m X^T(\hat y - y)
$$

#### 🔹 Bước 4 – Cập nhật tham số
$$
\theta := \theta - \eta\nabla_\theta L
$$

---

### 🌼 3️⃣ Ý nghĩa hình học và tối ưu hóa

- Mỗi dòng của $X$ là một vector đặc trưng của một mẫu.  
- Tích $X \theta$ là tổng hợp tuyến tính → tính đồng thời nhiều mẫu.  
- Gradient $\frac1m X^T(\hat y - y)$ là trung bình hướng sai số → cập nhật tham số theo hướng giảm mất mát toàn batch.  

---

### 🪞 4️⃣ Ví dụ cụ thể (từ slides)

Với $m = 2$:
$$
X = 
\begin{bmatrix}
1 & 1.5 & 0.2\\
1 & 4.1 & 1.3
\end{bmatrix},
\quad 
y = \begin{bmatrix}0\\1\end{bmatrix},
\quad 
\theta = 
\begin{bmatrix}
0.1\\0.5\\-0.1
\end{bmatrix}
$$

Tính:
$$
z = X\theta = 
\begin{bmatrix}
0.83\\2.02
\end{bmatrix},\quad 
\hat y = \sigma(z) = 
\begin{bmatrix}
0.6963\\0.8828
\end{bmatrix}
$$

Mất mát:
$$
L = -\frac12
\big[\log(1-0.6963)+\log(0.8828)\big]
≈ 0.65815
$$

Gradient:
$$
\nabla_\theta L = \frac12 X^T(\hat y - y)
=
\begin{bmatrix}
0.2896\\0.2822\\-0.0064
\end{bmatrix}
$$

Cập nhật tham số:
$$
\theta_{new} = \theta - \eta\nabla_\theta L
\Rightarrow 
\begin{bmatrix}
0.0971\\0.4971\\-0.099
\end{bmatrix}
\quad(\eta = 0.01)
$$  
📊 Đây chính là vector hóa phiên bản **mini-batch (m = 2)**.

---

### 🌺 5️⃣ Vectorization toàn bộ tập dữ liệu (*Batch Gradient Descent*)

Khi dữ liệu gồm \(N\) mẫu, ta tổng quát hóa:
$$
Z = X\theta,\qquad 
\hat Y = \sigma(Z)
\]
\[
L(\theta)
 = -\frac1N\sum_{i=1}^N[y^{(i)}\log\hat y^{(i)}+(1-y^{(i)})\log(1-\hat y^{(i)})]
\]
\[
\nabla_\theta L = \frac1N X^T(\hat Y - Y)
\]
\[
\theta := \theta - \eta\nabla_\theta L
$$

Ví dụ trong slides:
$$
X =
\begin{bmatrix}
1 & 1.4 & 0.2\\
1 & 1.5 & 0.2\\
1 & 3.0 & 1.1\\
1 & 4.1 & 1.3
\end{bmatrix},
\quad 
y =
\begin{bmatrix}
0\\0\\1\\1
\end{bmatrix}
\]
\[
Z = X\theta =
\begin{bmatrix}
0.78\\0.83\\1.49\\2.02
\end{bmatrix}
\Rightarrow 
\hat y =
\begin{bmatrix}
0.6856\\0.6963\\0.8160\\0.8828
\end{bmatrix}
\]
\[
L ≈ 0.6691,\qquad 
\nabla_\theta L =
\begin{bmatrix}
0.2702\\0.2431\\-0.019
\end{bmatrix}
\Rightarrow 
\theta_{new} =
\begin{bmatrix}
0.0971\\0.4971\\-0.099
\end{bmatrix}
$$  
📘 Toàn bộ các mẫu được xử lý đồng thời, giúp **ổn định gradient và hội tụ nhanh**.

---

### 🌸 6️⃣ Tổng hợp so sánh ba cấp độ vector hóa

| Cấp độ | Ký hiệu | Quy mô | Công thức gradient | Đặc điểm |
|:--:|:--|:--|:--|:--|
| 1️⃣ | One sample | 1 mẫu | \(\nabla_\theta L = x(\hat y-y)\) | Cập nhật chậm, nhiễu lớn |
| 2️⃣ | Mini-batch | m mẫu | \(\nabla_\theta L = \frac1m X^T(\hat y-y)\) | Cân bằng tốc độ và độ chính xác |
| 3️⃣ | Full batch | N mẫu | \(\nabla_\theta L = \frac1N X^T(\hat y-y)\) | Ổn định nhất nhưng tốn bộ nhớ |

---

### 🪶 7️⃣ Ý nghĩa tối ưu hóa và liên hệ thực tiễn

- **Batch gradient descent** phù hợp cho dataset nhỏ và học toàn bộ một lần.  
- **Mini-batch** là chuẩn trong Deep Learning – kết hợp hiệu năng GPU và độ ổn định hội tụ.  
- **Stochastic (one sample)** phù hợp dữ liệu stream hoặc online learning.

---

### 💎 8️⃣ Mã Python vector hóa (Numpy demo)

```python
import numpy as np

def sigmoid(z):
    return 1 / (1 + np.exp(-z))

def vectorized_gradient(X, y, theta):
    m = X.shape[0]
    y_hat = sigmoid(X @ theta)
    grad = (1/m) * (X.T @ (y_hat - y))
    return grad

def train(X, y, lr=0.1, epochs=1000):
    theta = np.zeros(X.shape[1])
    for i in range(epochs):
        grad = vectorized_gradient(X, y, theta)
        theta -= lr * grad
        if i % 200 == 0:
            loss = -np.mean(y*np.log(sigmoid(X@theta)) + (1-y)*np.log(1-sigmoid(X@theta)))
            print(f"Iter {i:4d} | Loss={loss:.6f}")
    return theta
```

---

## 💜 Phần 3 – Hàm kích hoạt (Activation Functions) và Hành vi Gradient

---

### 🌷 1️⃣ Tại sao cần hàm kích hoạt?

Trong mô hình Logistic Regression hoặc mạng nơ-ron, phần tuyến tính $z = w^T x + b$ chỉ tạo ra **đường/hyperplane phân chia tuyến tính**.  
Nếu ta giữ nguyên $z$, mô hình chỉ học được quan hệ **tuyến tính đơn giản** giữa đặc trưng và nhãn.  

👉 Hàm kích hoạt (activation function) tạo **phi tuyến tính (non-linearity)**, cho phép mô hình mô tả mối quan hệ phức tạp hơn, mô phỏng cách “kích hoạt neuron” trong não người.

Mỗi hàm kích hoạt ánh xạ đầu ra \(z\) sang một khoảng giá trị xác định và điều khiển cách gradient lan truyền trong quá trình học.

---

### 🌸 2️⃣ Hàm sigmoid – nền tảng của Logistic Regression

$$
\sigma(z) = \frac{1}{1 + e^{-z}}
$$

#### ✨ Đặc điểm:
- Đầu ra luôn nằm trong khoảng \((0,1)\) → dễ diễn giải như xác suất.  
- Đạo hàm:  
  $$
  \sigma'(z) = \sigma(z) \big(1 - \sigma(z)\big)
  $$
- Khi $z$ rất lớn hoặc rất nhỏ, $\sigma'(z)$ tiến gần 0 → gây **hiện tượng vanishing gradient**.

#### 🎨 Ví dụ trực quan:
- Nếu $z=0$ → $\sigma(z)=0.5$  
- Nếu $z=5$ → $\sigma(z)\approx0.993$  
- Nếu $z=-5$ → $\sigma(z)\approx0.0067$

👉 Điều này giải thích tại sao mô hình Logistic Regression “mềm” ở giữa (xác suất thay đổi mạnh quanh $z=0$ và “bão hòa” ở hai đầu.

---

### 🪷 3️⃣ Hàm tanh – cải tiến từ sigmoid

$$
\tanh(z) = \frac{e^z - e^{-z}}{e^z + e^{-z}}
$$

#### ✨ Đặc điểm:
- Đầu ra trong khoảng $(-1,1)$ thay vì $(0,1)$.  
- Trung tâm ở 0, nên gradient lan truyền tốt hơn (ít bias hơn).  
- Đạo hàm:  
  $$
  \tanh'(z) = 1 - \tanh^2(z)
  $$
- Tuy nhiên, khi $|z|$ quá lớn → đạo hàm vẫn gần 0 → vẫn có hiện tượng **vanishing gradient**.

#### 🎯 So sánh sigmoid vs tanh:
| Đặc tính | Sigmoid | tanh |
|:--|:--|:--|
| Khoảng giá trị | (0,1) | (-1,1) |
| Trung tâm tại 0 | ❌ | ✅ |
| Tốc độ hội tụ | Chậm hơn | Nhanh hơn |
| Vanishing Gradient | Có | Có (nhưng nhẹ hơn) |
| Ứng dụng chính | Logistic Regression, Output layer | Hidden layers trong MLP |

---

### 🧮 4️⃣ Mối liên hệ giữa Sigmoid và Gradient Descent

Với Logistic Regression:
$$
\hat y = \sigma(z) = \frac{1}{1+e^{-(w^Tx+b)}}
$$

Đạo hàm theo $w$:
$$
\frac{\partial L}{\partial w} = ( \hat y - y )x
$$

Khi $\sigma'(z)$ rất nhỏ (ví dụ khi $|z| > 5\$, gradient gần như bằng 0 → mô hình gần như **ngừng học**.  
Đó là lý do khi áp dụng sigmoid trong các mạng sâu (Deep Neural Networks), gradient từ các tầng cuối sẽ **“tan biến” dần về 0**, khiến việc huấn luyện khó khăn.

---

### 🩵 5️⃣ So sánh hành vi gradient qua biểu đồ (theo slides)

Hình từ slides minh họa:

| Hàm | Miền hoạt động mạnh | Khu vực gradient nhỏ |
|:--|:--|:--|
| Sigmoid | quanh z = 0 | z < −4 hoặc z > 4 |
| tanh | −2 < z < 2 | |z| > 3 |
| ReLU | z > 0 | z < 0 (gradient = 0) |

Cụ thể:
- Sigmoid và tanh đều có vùng “bão hòa” ở hai đầu → gradient nhỏ, cập nhật chậm.  
- ReLU (Rectified Linear Unit) khắc phục nhược điểm đó bằng cách **giữ gradient = 1 khi z > 0**.

---

### 🌿 6️⃣ ReLU – bước ngoặt cho Deep Learning

$$
\text{ReLU}(z) = \max(0, z)
\]
\[
\text{ReLU}'(z) = 
\begin{cases}
1, & z > 0 \\
0, & z \le 0
\end{cases}
$$

#### ✨ Ưu điểm:
- Gradient không bị bão hòa khi \(z > 0\).  
- Tính toán cực nhanh (chỉ cần so sánh).  
- Giúp mạng sâu học nhanh hơn nhiều so với sigmoid/tanh.

#### ⚠️ Nhược điểm:
- Khi \(z \le 0\), gradient = 0 → neuron “chết” (dead ReLU).  
- Cần kỹ thuật khởi tạo và learning rate hợp lý.

---

### 🌈 7️⃣ Hàm Leaky ReLU và các biến thể

Để tránh “dead ReLU”, ta cho phép gradient nhỏ ở vùng âm:
$$
\text{LeakyReLU}(z) =
\begin{cases}
z, & z>0\\
\alpha z, & z\le0
\end{cases}
$$

với $\alpha \approx 0.01$.  
Các biến thể khác: **ELU, GELU, Swish** (dùng trong BERT/Transformer).

---

### 💫 8️⃣ Tác động của activation lên loss surface

Khi dùng sigmoid, loss surface (bề mặt hàm mất mát) cong hơn nhiều, dẫn đến:
- Gradient nhỏ → bước đi của optimizer ngắn.  
- Có thể mắc kẹt trong vùng bằng phẳng → chậm hội tụ.

ReLU tạo loss surface “mềm hơn” (piecewise linear), cho phép gradient descent **di chuyển nhanh và ổn định hơn**.

---

### 🪞 9️⃣ Minh họa code so sánh các activation

```python
import numpy as np
import matplotlib.pyplot as plt

z = np.linspace(-6, 6, 400)
sigmoid = 1 / (1 + np.exp(-z))
tanh = np.tanh(z)
relu = np.maximum(0, z)
leaky = np.where(z > 0, z, 0.01*z)

plt.figure(figsize=(8,5))
plt.plot(z, sigmoid, label='Sigmoid')
plt.plot(z, tanh, label='tanh')
plt.plot(z, relu, label='ReLU')
plt.plot(z, leaky, label='Leaky ReLU')
plt.title("Activation Functions")
plt.legend()
plt.grid(True)
plt.show()
```

### 🪶 9️⃣ Tác động lên loss surface

- **Sigmoid** → loss surface cong, dễ kẹt ở vùng bằng phẳng.  
- **ReLU** → loss surface “mềm”, gradient descent di chuyển nhanh hơn.  
- **tanh** → cân bằng giữa hai loại.  

---

## 💜 🔟 Kết luận phần 3

| Hàm | Ưu điểm | Nhược điểm | Ứng dụng chính |
|:--|:--|:--|:--|
| **Sigmoid** | Diễn giải xác suất dễ, trơn | Vanishing gradient | Output nhị phân |
| **tanh** | Trung tâm tại 0, hội tụ nhanh | Vanishing gradient nhẹ | Hidden layer |
| **ReLU** | Tính nhanh, không bão hòa | Dead neurons | Hidden layer (chuẩn) |
| **Leaky ReLU** | Giữ gradient vùng âm | Cần điều chỉnh α | CNN/Transformer |

Từ Logistic Regression đến Deep Learning, hành vi gradient là **trái tim của quá trình huấn luyện**.  
Hiểu rõ các hàm kích hoạt giúp chọn đúng “cầu nối phi tuyến” cho từng tầng mạng — đảm bảo mô hình **vừa hội tụ nhanh, vừa tổng quát tốt.**


---


---

## 🌿 Phần 4 – Vectorization Summary & Gradient Formulas

> 🎯 *Mục tiêu:* Hiểu cách biểu diễn toàn bộ công thức Logistic Regression dưới dạng **ma trận (vectorized)**  
> để chuẩn bị bước sang **cài đặt thực tế bằng Python (Phần 4).**

---

### 💡 1️⃣ Tư duy vector hóa

Thay vì tính toán từng mẫu dữ liệu bằng vòng `for`,  
ta gom tất cả dữ liệu đầu vào thành **ma trận \( X \)**:

$$
X =
\begin{bmatrix}
x_1^{(1)} & x_2^{(1)} & \cdots & x_n^{(1)} \\
x_1^{(2)} & x_2^{(2)} & \cdots & x_n^{(2)} \\
\vdots & \vdots & \ddots & \vdots \\
x_1^{(m)} & x_2^{(m)} & \cdots & x_n^{(m)}
\end{bmatrix}
$$

- $m$: số lượng mẫu  
- $n$: số đặc trưng (features)  
- $X \in \mathbb{R}^{m \times n}$

Ta biểu diễn toàn bộ phép tính như sau:

$$
Z = XW + b
\]
\[
\hat{Y} = \sigma(Z)
$$

→ Trong đó,  
- $W \in \mathbb{R}^{n \times 1}$: vector trọng số  
- $b \in \mathbb{R}$: bias  
- $\sigma(z) = \frac{1}{1 + e^{-z}}$: hàm kích hoạt sigmoid

---

### 🧠 2️⃣ Forward Propagation (Lan truyền thuận)

Đây là bước tính **dự đoán đầu ra** dựa trên đầu vào \( X \):

$$
\hat{Y} = \sigma(XW + b)
$$

Kết quả $\hat{Y} \in \mathbb{R}^{m \times 1}$  
→ chứa xác suất “mỗi mẫu thuộc lớp 1”.

---

### 💔 3️⃣ Binary Cross-Entropy Loss (Hàm mất mát)

Hàm BCE đo độ sai lệch giữa dự đoán \( \hat{Y} \) và giá trị thật \( Y \):

$$
J(W, b) = -\frac{1}{m}\sum_{i=1}^{m} \Big( y^{(i)}\log \hat{y}^{(i)} + (1 - y^{(i)})\log (1 - \hat{y}^{(i)}) \Big)
$$

Biểu diễn dạng vector hóa:

$$
J = -\frac{1}{m}\Big( Y^T \log(\hat{Y}) + (1 - Y)^T \log(1 - \hat{Y}) \Big)
$$

> 💬 *Ý nghĩa:*  
> - Khi dự đoán đúng (gần 1 nếu y=1, gần 0 nếu y=0) → loss nhỏ  
> - Khi sai lệch nhiều → loss lớn (phạt mạnh)

---

### ⚙️ 4️⃣ Gradient Derivation (Đạo hàm vector hóa)

Để cập nhật $W$ và $b$, ta cần tính gradient của hàm mất mát:

$$
\frac{\partial J}{\partial W} = \frac{1}{m} X^T (\hat{Y} - Y)
\]
\[
\frac{\partial J}{\partial b} = \frac{1}{m} \sum_{i=1}^{m} (\hat{y}^{(i)} - y^{(i)})
$$

> 🧩 *Giải thích:*  
> - $\hat{Y} - Y)$ là vector sai số cho từng mẫu  
> - $X^T(\hat{Y} - Y)$ gộp toàn bộ sai số của các feature  
> - Chia cho $m$ để tính trung bình gradient trên batch

---

### 🔁 5️⃣ Gradient Descent Update Rules

Sau khi có đạo hàm, ta cập nhật trọng số theo hướng giảm loss:

$$
W := W - \alpha \frac{\partial J}{\partial W}
\]
\[
b := b - \alpha \frac{\partial J}{\partial b}
$$

- $\alpha$: learning rate  
- Cập nhật lặp đi lặp lại → mô hình hội tụ dần.

---

### 🪄 6️⃣ So sánh: Loop vs Vectorization

| Tiêu chí | Cách cũ (loop từng mẫu) | Vector hóa |
|:--|:--|:--|
| Cấu trúc | `for i in range(m): ...` | 1 dòng ma trận |
| Tốc độ | Chậm, tốn CPU | Nhanh, tối ưu GPU |
| Code | Dài, khó debug | Ngắn gọn, dễ hiểu |
| Mở rộng | Giới hạn batch nhỏ | Dễ mở rộng mini-batch / full-batch |
| Ứng dụng | Demo toy model | Deep Learning thực chiến |

> 🌈 *Vectorization = học “song song” tất cả mẫu cùng lúc → tốc độ tăng theo cấp số nhân.*

---

### 🌸 7️⃣ Tóm lược toàn công thức huấn luyện

| Bước | Công thức | Ý nghĩa |
|:--|:--|:--|
| **1. Forward** | $\hat{Y} = \sigma(XW + b)$ | Dự đoán xác suất |
| **2. Loss** | $J = -\frac{1}{m}(Y^T\log\hat{Y} + (1-Y)^T\log(1-\hat{Y}))$ | Sai lệch dự đoán |
| **3. Gradient** | $dW = \frac{1}{m}X^T(\hat{Y}-Y)$, $db = \frac{1}{m}\sum(\hat{Y}-Y)$ | Đạo hàm |
| **4. Update** | $W := W - \alpha dW,\ b := b - \alpha db$ | Cập nhật trọng số |

---

### 💫 8️⃣ Minh họa trực quan



## 💻 Phần 5 – Code minh họa (Vectorized Implementation)

```python
import numpy as np

def sigmoid(z):
    return 1 / (1 + np.exp(-z))

def forward(X, W, b):
    Z = X @ W + b
    return sigmoid(Z)

def bce_loss(Y, Y_hat):
    eps = 1e-12
    Y_hat = np.clip(Y_hat, eps, 1 - eps)
    return -np.mean(Y * np.log(Y_hat) + (1 - Y) * np.log(1 - Y_hat))

def gradients(X, Y, Y_hat):
    m = X.shape[0]
    dW = (1/m) * (X.T @ (Y_hat - Y))
    db = (1/m) * np.sum(Y_hat - Y)
    return dW, db

def train(X, Y, lr=0.1, epochs=1000):
    n = X.shape[1]
    W = np.zeros((n, 1))
    b = 0.0
    for t in range(epochs):
        Y_hat = forward(X, W, b)
        loss = bce_loss(Y, Y_hat)
        dW, db = gradients(X, Y, Y_hat)
        W -= lr * dW
        b -= lr * db
        if t % 200 == 0:
            print(f"Epoch {t:4d} | Loss = {loss:.6f}")
    return W, b

# Demo mini
if __name__ == "__main__":
    np.random.seed(42)
    X = np.random.randn(6, 2)
    Y = np.array([[0],[1],[0],[1],[1],[0]])
    W, b = train(X, Y, lr=0.3, epochs=1000)
    preds = forward(X, W, b)
    print("\n✅ Final weights:\n", np.round(W,3), "\nBias:", round(b,3))
    print("\n🔮 Predictions:", np.round(preds,3).ravel())
    print("📊 Classes:", (preds>=0.5).astype(int).ravel())

```
---

## 🌈 Phần 6 – Ứng dụng và phân tích

### 🚀 Batch vs. Mini-batch Gradient

- **Batch GD:** cập nhật sau mỗi epoch → ổn định, nhưng chậm.  
- **Mini-batch GD:** cập nhật từng nhóm nhỏ → nhanh hơn, noise giúp tránh local minima.

---

### 🔍 Ưu điểm vectorization

| Hạng mục | Không vector | Vector hóa |
|-----------|---------------|-------------|
| ⏱️ **Tốc độ** | Chậm, phải dùng loop | Nhanh, dùng phép nhân ma trận |
| 💻 **Code** | Dài dòng, khó mở rộng | Gọn gàng, dễ debug |
| 🧮 **Dễ mở rộng** | Giới hạn CPU | Tối ưu GPU/TPU |
| 🎯 **Ứng dụng** | Model toy nhỏ | Deep Learning thực chiến |

---

### 💡 Kiến thức mở rộng

Vectorization là nền tảng cho:

- **🧠 Neural Networks:** mọi layer đều là phép nhân ma trận.  
- **🔁 Backpropagation:** lan truyền gradient qua từng layer.  
- **⚙️ Autograd engine (PyTorch/TF):** tính toán đạo hàm tự động từ vectorized ops.

> 👉 Nhờ vectorization, các mô hình hiện đại có thể huấn luyện hàng trăm triệu tham số chỉ trong vài giờ trên GPU.

---

## 💜 Kết luận & Hẹn gặp tuần sau

Tuần này, chúng ta đã:

✅ Nắm được cách vector hóa Logistic Regression  
✅ Hiểu rõ cơ chế gradient descent và binary cross-entropy  
✅ Viết pipeline hoàn chỉnh trong Python và kiểm thử mini demo  

Tuần tới, ta sẽ khám phá:

🌸 **Multi-feature Logistic Regression**  
🌸 **Regularization (L1/L2)** – để tránh overfitting  
🌸 **Feature scaling & normalization** – giúp mô hình học ổn định hơn  

> ✨ “Vectorization không chỉ giúp máy tính chạy nhanh hơn —  
> mà còn giúp ta hiểu rõ hơn về bản chất của học máy.” 🌿

---

## 📚 Tài nguyên  

- 👨‍🏫 **Coding cho bài giảng của thầy Quang–Vinh Đinh (Google Drive)**  
 
  🔗 [Mở thư mục Google Drive tại đây](https://drive.google.com/drive/folders/1haJTnN3AFM4ZpuBJZfrKK-fhH_GGj3RP?usp=sharing)    



💡 *Hẹn gặp ở bài giảng tiếp theo của tuần 2 – Khi chúng ta dive sâu hơn vào Deep Learning nhé!* 🚀    



🏠 [Về trang chủ]({{ '/' | relative_url }})    




